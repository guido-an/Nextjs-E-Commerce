import CartIcon from './CartIcon'

const Header = props => (
  <header>
    header
    {console.log()}
    <CartIcon />
    <style jsx>{`
      background-color: #f7f7f7;
      padding: 40px;
    `}</style>
  </header>
);

export default Header;
